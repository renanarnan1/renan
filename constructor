length, charAt, getChars

String S1="hello there";
Char[] charArray = new char[5]
//length

System.out.printf("length is %d", s1.length());
//charAT

for(int count = S1.length()-1; count >=0; count--)
system.out.printf("%c", S1.charAt(count);
//getChars
S1.getChars(0,5,charArray,0);
for(charcharacter:charArray)
system.out.print(character)


String Constructors
char[] charArray={'b','i','r','t','h','','d','a','y'};
String s = new String ("hello");
String s1 = new String ();
String s2 = new String (s);
String s3 = new String (charArray);
String s4 = new String (charrArray,6,3);


comparing strings
= equals
if (s1.equals("hello"));

= equalsIgnoreCase
if(s3.equalsIgnoreCase(S4));

=compareTo(S2);   0 if equal - negative if < - negative if >

region matches
if(S3.regionMatches(0,S4,0,5);
if(s3,regionsMatches(true,0,S4,0,5);


fundamentals of characters and Strings

Characters literals an integer value represented as a character in single quotes.

String is a sequence of characters.

String literals are written as a sequence of characters in double quotes

a string maybe assigned to a string reference
String color = "blue",
